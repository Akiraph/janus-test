name = "execution"
public_root = "interface/mod.rs"
owned_collections = [
  "rounds",
  "tool_calls",
  "plan_versions",
  "compact_summaries",
  "context_versions",
]
publishes = [
  "round.changed",
  "tool_call.created",
  "tool_call.changed",
  "context.changed",
]
allowed_module_dependencies = ["models", "projects", "runtime", "sessions", "workspace"]
