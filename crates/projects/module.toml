name = "projects"
public_root = "interface.rs"
owned_tables = [
  "projects",
  "github_credentials",
  "project_runtime_configs",
  "project_runtime_secrets",
  "project_egress_rules",
  "project_cli_configs",
]
publishes = [
  "project.changed",
  "project.main_revision_changed",
]
allowed_module_dependencies = ["runtime", "workspace"]
