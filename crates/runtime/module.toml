name = "runtime"
public_root = "interface.rs"
owned_tables = [
  "runtimes",
  "log_streams",
  "jobs",
  "services",
  "terminals",
  "runtime_access_tickets",
]
publishes = ["runtime.changed", "job.changed", "service.changed", "terminal.changed"]
allowed_module_dependencies = []
