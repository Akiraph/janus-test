name = "runtime"
public_root = "interface.rs"
owned_tables = [
  "runtimes",
  "log_streams",
  "async_tasks",
  "terminals",
  "runtime_access_tickets",
]
publishes = ["runtime.changed", "async_task.changed", "terminal.changed"]
allowed_module_dependencies = []
