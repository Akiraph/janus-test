name = "runtime"
public_root = "interface.rs"
owned_tables = [
  "runtimes",
  "log_streams",
  "jobs",
  "services",
  "runtime_ports",
  "terminals",
  "runtime_access_tickets",
]
publishes = ["job.changed", "service.changed", "terminal.changed"]
allowed_module_dependencies = []
specs = []
tests = []
