name = "source-control"
public_root = "interface.rs"
owned_tables = [
  "project_git_state",
  "git_update_conflicts",
  "git_update_conflict_paths",
]
publishes = [
  "git.state_changed",
  "git.update_conflict_changed",
]
allowed_module_dependencies = ["workspace"]
