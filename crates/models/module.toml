name = "models"
public_root = "interface.rs"
owned_collections = [
  "model_providers",
  "models",
  "model_failover",
  "model_attempts",
  "model_usage_ledger",
  "automation_settings",
]
publishes = ["model_config.changed"]
allowed_module_dependencies = []
