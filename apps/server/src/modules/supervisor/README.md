# supervisor

Owns rounds, tool calls, context, plans, and Turn completion decisions; it does not own messages, processes, or provider credentials. The current interface owns no state. Allowed dependencies are models, projects, runtime, sessions, and workspace-sync.
