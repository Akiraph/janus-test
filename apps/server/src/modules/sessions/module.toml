name = "sessions"
public_root = "interface.rs"
owned_tables = [
  "sessions",
  "turns",
  "messages",
  "timeline_items",
  "checkpoints",
  "uploads",
  "attachments",
  "message_attachments",
]
publishes = [
  "session.changed",
  "session.deleted",
  "turn.created",
  "turn.status_changed",
  "timeline.item_created",
  "timeline.item_updated",
  "checkpoint.created",
]
allowed_module_dependencies = ["workspace-sync", "projects"]
specs = []
tests = []
