# sessions

Owns Session identity, Turns, messages, checkpoints, and history relationships; it does not own model rounds or workspace bytes. The current interface owns no state and has no Module dependencies.
