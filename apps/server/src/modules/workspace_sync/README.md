# workspace-sync

Owns Managed Content, revisions, propagation, conflicts, and atomic file recovery; it does not own user Git operations or Session messages. The current interface owns no state. Its allowed Module dependency is projects.
