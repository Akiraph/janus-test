# 能力模块

能力模块拥有自己的术语、状态机、持久化和公开接口。模块之间只能使用公开接口，不读取对方的表，不依赖对方的私有实现，也不通过全局事件互相发命令。

当前目录仍保留旧的聚合方式，后续会把 `projects` 拆成 Project、Source Control；Workspace 已迁移到 `crates/workspace`。执行相关能力统一放进 `execution`，不再新增历史聚合名。迁移时优先移动所有权和接口，再移动文件；不要在旧模块里继续堆功能来延长过渡期。
