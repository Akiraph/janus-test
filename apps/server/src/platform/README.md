# 基础设施

这里只维护 server 特有的部署密钥、Workspace 路径约束和进程 shell 工具，以及仍属于 server 的业务 ID。数据库连接、短事务、受控 Blob 存储、操作记录和公开事件已经迁移到 `janus-infrastructure`；新代码直接依赖那个 crate，不要在这里增加转发模块。

基础设施不能为了方便读取业务表、拼装领域事件或保存业务状态。业务规则应留在拥有它的能力；如果某个抽象只被一个能力使用，就不要把它提升到这里。
