# Models Feature

This feature manages Provider configuration, model selection, parameters, and usage views. It calls the public models API and must not copy Session or Execution retry, waiting, or completion state into the frontend.
