# Security Feature

This feature displays Owner security settings and recovery entry points. The Identity capability stores and validates security credentials; the UI handles input, state, and error presentation only.
