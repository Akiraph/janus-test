# Projects Feature

This feature owns the Project list and the overall Project workspace interaction. Project metadata, Source Control, Workspace, Session, and Runtime panels retain their backend capability boundaries; this feature composes the operator view without owning another capability's state machine.
