# Project Workspace

This is the Project-level operator surface for files, Git, the Session timeline, and the Project Terminal. Components coordinate through public queries and commands; they must not bypass the transport layer to assemble requests or treat a Session workspace as an arbitrary terminal directory.
